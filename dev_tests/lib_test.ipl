# This requires ILI with the examplelib library

use examplelib


example_a = examplelib.a # Should be 3

out("a")
out(example_a)

example_c = examplelib.c # Should be 13

out("c")
out(example_c)

out("Adding a and c")
a_c = examplelib.add(example_a, example_c) # Should be 16

out("a_c")
out(a_c)

test = examplelib.Test()

number = test.number # Should be 10
out("test number")
out(number)