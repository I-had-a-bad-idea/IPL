import class_test_base_class

class Test : Base
    self.number = 20
    self.string = "Test"

    def add(a,b)
        return a + b
    
    def greet()
        out("Hello World")
    
    

t = Test()

z = t.add(3, 5)
out("z")
out(z)

t.greet()

i = 0
while i <= 5
    out("t.number")
    out(t.number)

    t.number = t.number + 1
    i = i + 1
    
    out("i")
    out(i)

out(t.exists)

t.name()