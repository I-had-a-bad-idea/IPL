import class_test_base_class.ipl

class Test : Base
    self.number = 20
    self.string = "Test"

    def greet()
        out("Hello World")
    
    def add(a, b)
        return a + b

t = Test()

z = t.add(3, 5)
out("z")
out(z)

t.greet()

i = 0
while i <= 5
    out("t.number")
    out(t.number)

    t.number = t.number + 1
    i = i + 1
    
    out("i")
    out(i)

out(t.exists)

t.name()